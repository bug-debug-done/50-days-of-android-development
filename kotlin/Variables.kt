fun main(args: Array<String>) {
    val x="hello it cannot be changed because it is a val"
    var y="hello it can be changed because it is a var"
    println(x)
        println(y)


        // x="dsdvsdsv" //it gives error
        y="var value has been changed"
            println(y)



}